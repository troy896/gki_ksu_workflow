name: 'Inject Kernel Configs'
description: 'Applies standard networking, performance, and KSU flags'
inputs:
  defconfig_path:
    description: 'Path to the defconfig file'
    required: true
  build_config_path:
    description: 'Path to the build.config.gki file'
    required: true
  kernel_version:
    description: 'Kernel version'
    required: false
    default: ''
  sub_level:
    description: 'Sublevel'
    required: false
    default: '0'
  variant:
    description: 'KSU variant (MidoriSU-KX, MidoriSU-XX, MidoriSU-RX, etc.)'
    required: true
  midorisu_rx_hook_mode:
    description: 'MidoriSU-RX hook mode (manual, tracepoint)'
    required: false
    default: 'manual'
  midorisu_xx_hook_mode:
    description: 'MidoriSU-XX hook mode (branch link hijacking, syscall table tampering, manual)'
    required: false
    default: 'branch link hijacking'
  ksu_debug:
    description: 'Set to "true" to enable KSU debug'
    required: false
    default: 'false'
  enable_susfs:
    description: 'Set to "true" to inject SUSFS configs'
    required: false
    default: 'false'
  use_droidspaces:
    description: 'Set to "true" to inject Droidspaces configs'
    required: false
    default: 'false'
  use_rekernel:
    description: 'Set to "true" to inject ReKernel configs'
    required: false
    default: 'false'
  use_adios:
    description: 'Set to "true" to inject ADIOS'
    required: false
    default: 'false'
  use_lz4_zstd:
    description: 'Set to "true" to enable the LZ4/ZSTD ZRAM backends (kernel 6.12 only)'
    required: false
    default: 'false'
  localversion:
    description: 'Kernel Localversion String'
    required: true

runs:
  using: "composite"
  steps:
    - name: Apply Configurations
      shell: bash
      run: |
        DEFCONFIG="${{ inputs.defconfig_path }}"

        sed -i '/KMI_SYMBOL_LIST_STRICT_MODE/d' "${{ inputs.build_config_path }}" || true
        sed -i '/CONFIG_LOCALVERSION/d' $DEFCONFIG
        echo "CONFIG_LOCALVERSION=\"${{ inputs.localversion }}\"" >> $DEFCONFIG
        
        cat >> $DEFCONFIG <<-'EOF'
        CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y
        CONFIG_LOG_BUF_SHIFT=23
        CONFIG_TMPFS_XATTR=y
        CONFIG_TMPFS_POSIX_ACL=y
        CONFIG_IP_NF_TARGET_TTL=y
        CONFIG_IP6_NF_TARGET_HL=y
        CONFIG_IP6_NF_MATCH_HL=y
        CONFIG_TCP_CONG_ADVANCED=y
        CONFIG_TCP_CONG_BBR=y
        CONFIG_TCP_CONG_BBR3=y
        CONFIG_DEFAULT_BBR3=y
        CONFIG_NET_SCH_FQ=y
        CONFIG_NET_SCH_CODEL=y
        CONFIG_NET_SCH_FQ_CODEL=y
        CONFIG_NET_SCH_INGRESS=y
        CONFIG_NET_SCH_DEFAULT=y
        CONFIG_DEFAULT_FQ_CODEL=y
        CONFIG_ZSTD_COMPRESS=y
        CONFIG_ZSTD_DECOMPRESS=y
        CONFIG_BLK_DEV_ZRAM=y
        CONFIG_ZRAM_DEF_COMP_LZ4=y
        CONFIG_ZRAM_WRITEBACK=y
        CONFIG_ZRAM_GS_WRITEBACK=y
        CONFIG_ZRAM_MEMORY_TRACKING=y
        CONFIG_SWAP=y
        CONFIG_RD_LZMA=y
        CONFIG_IP_SET=y
        CONFIG_NETFILTER_XT_SET=y
        CONFIG_IP_SET_MAX=65534
        CONFIG_IP_SET_BITMAP_IP=y
        CONFIG_IP_SET_BITMAP_IPMAC=y
        CONFIG_IP_SET_BITMAP_PORT=y
        CONFIG_IP_SET_HASH_IP=y
        CONFIG_IP_SET_HASH_IPMARK=y
        CONFIG_IP_SET_HASH_IPPORT=y
        CONFIG_IP_SET_HASH_IPPORTIP=y
        CONFIG_IP_SET_HASH_IPPORTNET=y
        CONFIG_IP_SET_HASH_IPMAC=y
        CONFIG_IP_SET_HASH_MAC=y
        CONFIG_IP_SET_HASH_NETPORTNET=y
        CONFIG_IP_SET_HASH_NET=y
        CONFIG_IP_SET_HASH_NETNET=y
        CONFIG_IP_SET_HASH_NETPORT=y
        CONFIG_IP_SET_HASH_NETIFACE=y
        CONFIG_IP_SET_LIST_SET=y
        CONFIG_NETFILTER_XT_TARGET_REDIRECT=y
        CONFIG_NETFILTER_XT_TARGET_REJECT=y
        CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y
        CONFIG_NETFILTER_XT_MATCH_RECENT=y
        CONFIG_NETFILTER_XT_MARK=y
        CONFIG_NETFILTER_XT_CONNMARK=y
        CONFIG_IP6_NF_NAT=y
        CONFIG_IP6_NF_TARGET_REDIRECT=y
        CONFIG_BPF_STREAM_PARSER=y
        CONFIG_NOMOUNT=y
        CONFIG_SECTION_MISMATCH_WARN_ONLY=y
        CONFIG_MODULE_SIG=n
        # CONFIG_HEADERS_INSTALL is not set
        EOF
        
        case "${{ inputs.variant }}" in
          MidoriSU-XX)
            case "${{ inputs.midorisu_xx_hook_mode }}" in
              manual)
                cat >> $DEFCONFIG <<-'EOF'
        CONFIG_KSU_THRONE_TRACKER_ALWAYS_THREADED=y
        CONFIG_KSU_HOSTSREDIRECT=y
        # CONFIG_KSU_KPROBES_KSUD is not set
        # CONFIG_KSU_HACK_ARM64_BRANCH_LINK is not set
        # CONFIG_KSU_TAMPER_SYSCALL_TABLE is not set
        EOF
                ;;
              'syscall table tampering'|syscall-table|*syscall*|*tamper*)
                cat >> $DEFCONFIG <<-'EOF'
        CONFIG_KSU_THRONE_TRACKER_ALWAYS_THREADED=y
        CONFIG_KSU_HOSTSREDIRECT=y
        CONFIG_KSU_TAMPER_SYSCALL_TABLE=y
        # CONFIG_KSU_HACK_ARM64_BRANCH_LINK is not set
        EOF
                ;;
              *)
                cat >> $DEFCONFIG <<-'EOF'
        CONFIG_KSU_THRONE_TRACKER_ALWAYS_THREADED=y
        CONFIG_KSU_HOSTSREDIRECT=y
        CONFIG_KSU_HACK_ARM64_BRANCH_LINK=y
        # CONFIG_KSU_TAMPER_SYSCALL_TABLE is not set
        EOF
                ;;
            esac
            ;;
          MidoriSU-RX)
            if [ "${{ inputs.midorisu_rx_hook_mode }}" = "tracepoint" ]; then
              cat >> $DEFCONFIG <<-'EOF'
        CONFIG_KSU_TOOLKIT_SUPPORT=y
        CONFIG_KSU_TRACEPOINT_HOOK=y
        # CONFIG_KSU_MANUAL_HOOK is not set
        EOF
            else
              cat >> $DEFCONFIG <<-'EOF'
        CONFIG_KSU_TOOLKIT_SUPPORT=y
        CONFIG_KSU_MANUAL_HOOK=y
        # CONFIG_KSU_TRACEPOINT_HOOK is not set
        # CONFIG_KSU_MANUAL_HOOK_AUTO_SETUID_HOOK is not set
        # CONFIG_KSU_MANUAL_HOOK_AUTO_INITRC_HOOK is not set
        EOF
            fi
            if [ "${{ inputs.enable_susfs }}" = "true" ]; then
              cat >> $DEFCONFIG <<-'EOF'
        CONFIG_KSU_SUSFS=y
        EOF
            else
              cat >> $DEFCONFIG <<-'EOF'
        # CONFIG_KSU_SUSFS is not set
        EOF
            fi
            ;;
        esac
        
        if [ "${{ inputs.ksu_debug }}" = "true" ]; then
          cat >> $DEFCONFIG <<-'EOF'
        CONFIG_KSU_DEBUG=y
        EOF
        fi
        
        if [ "${{ inputs.use_droidspaces }}" = "true" ]; then
          cat >> $DEFCONFIG <<-'EOF'
        CONFIG_NOMOUNT=y
        EOF
        fi
        
        if [ "${{ inputs.use_rekernel }}" = "true" ]; then
          cat >> $DEFCONFIG <<-'EOF'
        CONFIG_REKERNEL=y
        CONFIG_REKERNEL_NETWORK=y
        CONFIG_REKERNEL_LEGACY_NETLINK=y
        EOF
        fi

        if [ "${{ inputs.use_adios }}" = "true" ] && [ "${{ inputs.kernel_version }}" != "6.1" ]; then
          cat >> $DEFCONFIG <<-'EOF'
        CONFIG_MQ_IOSCHED_ADIOS=y
        CONFIG_MQ_IOSCHED_DEFAULT_ADIOS=y
        EOF
        fi

        if [ "${{ inputs.use_lz4_zstd }}" = "true" ] && [ "${{ inputs.kernel_version }}" = "6.12" ]; then
          cat >> $DEFCONFIG <<-'EOF'
        CONFIG_ZRAM_BACKEND_LZ4=y
        CONFIG_ZRAM_BACKEND_ZSTD=y
        EOF
        fi
        
        sed -i 's/check_defconfig//' "${{ inputs.build_config_path }}" || true
